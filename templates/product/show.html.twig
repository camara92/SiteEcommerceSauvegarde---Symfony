{% extends 'base.html.twig' %}

{% block title %} {{ product.name }} -la boutique en ligne {% endblock %}
{%  block content  %}
    <div class="row">
        <div class="col-md-7">
            <img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid">
        </div>
    <div class="col-md-7">
        <h3>{{ product.name }} </h3>
        <p>{{ product.subtitle }}</p>
        <span class="product-page-price">{{ (product.price /100)|number_format(2, ',', '.')}}€</span>
        <hr>
        <p>{{ product.description }}</p>
        <hr>
        <button><a href="" class="btn btn-primary">Ajouter au panier </a></button>
    </div>
</div>

{%  endblock %}


